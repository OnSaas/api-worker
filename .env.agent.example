# Agent local environment template
# Copy to your local shell profile or CI secret manager.

# UI local dev (optional)
VITE_API_TARGET=http://localhost:8787
VITE_API_BASE=

# Agent runtime config
AGENT_CLOUD_URL=
AGENT_CLOUD_PASSWORD=
AGENT_LOCAL_PASSWORD=
